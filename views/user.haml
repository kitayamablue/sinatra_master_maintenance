%h2 作業者一覧
%div{:class => "containar"}
	%table{:border => 1, :class => "table table-striped table-hover table-bordered table-condensed"}
		%thead
			%tr
				%th ユーザーID
				%th ユーザー名
				%th ユーザーグループ 
				%th HV出庫ランク
				%th 社員コード
				%th 次ロケ表示
				%th 鮮度表示
				%th HV梱包ランク
		%tbody
			- @users.each do |user|
				%tr
					%td= user.UserID
					%td= user.UserName
					%td= user.UserGroup
					%td= user.Rank
					%td= user.EmployeeCode
					%td= user.LocView
					%td= user.LifeCheck
					%td= user.PackRank
:erb
		<%= will_paginate @users , :renderer => PaginateRenderer %>